apiVersion: v1
kind: Service
metadata:
  name: 
spec:
  selector:
    app: flaskapp
  ports:
  - protocol: TCP
    port: 80
    targetPort: 5000
  type: ClusterIP
---
apiVersion: apps/v1
kind: Deployment
metadata:
  name: app-deployment
spec:
  replicas: 
  selector:
    matchLabels:
      app: flaskapp
  template:
    metadata:
      labels:
        app: flaskapp
    spec:
      containers:
      - name: nginx
        image: nginx
        ports:
        - containerPort: 80

      - name: frontend
        image: 
        ports:
        - containerPort: 5000

      - name: backend
        image:
        ports:
        - containerPort: 5001

      - name: database
        image:
        ports:
        - containerPort:

        
