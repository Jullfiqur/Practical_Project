apiVersion: apps/v1
kind: Deployment
metadata:
  name: app-deployment
spec:
  replicas:
  selector:
    matchLabels:
      app: flaskapp
  template:
    metadata:
      labels:
        app: flaskapp
    spec:
      containers:
      - name: frontend
        image: jullfiqur/sfia2:frontend 
        ports:
        - containerPort: 5000
